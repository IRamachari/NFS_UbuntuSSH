name: UbuntuSSH
on: 
  workflow_dispatch:
defaults:
  run:
    shell: bash
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: anggit86/docker:cirrus
    steps:
    - name: Session Setup
      uses: mxschmitt/action-tmate@v3.11
      timeout-minutes: 300
      with:
        sudo: false
